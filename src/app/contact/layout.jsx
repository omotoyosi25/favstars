import React from 'react'

export const metadata = {
    title: 'contact information',
    description: 'more about our contact',
  }

export default function layout({children}) {
  return (
    <div>
            {children}
    </div>
  )
}